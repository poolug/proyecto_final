<%= form_with(model: housing_user, local: true) do |form| %>
  <% if housing_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(housing_user.errors.count, "error") %> prohibited this housing_user from being saved:</h2>

      <ul>
      <% housing_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :housing_id %>
    <%= form.text_field :housing_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
